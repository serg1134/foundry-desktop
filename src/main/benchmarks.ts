import { mkdir, readFile, writeFile } from 'node:fs/promises';
import { dirname, join } from 'node:path';
import type { WorkflowStep } from './agent';

export type BenchmarkCase={id:string;category:string;name:string;prompt:string;workflow?:WorkflowStep[]};
export type BenchmarkFailureStage='generation'|'compile'|'runtime'|'workflow'|'persistence'|'visual'|'unknown';
export type BenchmarkResult={id:string;benchmarkId:string;passed:boolean;durationMs:number;checksPassed:number;checksTotal:number;filesChanged:number;completedAt:string;projectId?:string;recheck?:boolean;failure?:string;failureStage?:BenchmarkFailureStage;repairAttempts?:number;repaired?:boolean;repairStages?:BenchmarkFailureStage[];campaignId?:string;campaignCycle?:number};
export type BenchmarkCampaignCycle={cycle:number;startedAt:string;completedAt:string;passed:number;total:number;successRate:number;qualified:boolean};
export type BenchmarkCampaign={id:string;status:'running'|'passed'|'failed'|'cancelled';startedAt:string;completedAt?:string;currentCycle:number;currentCase:number;totalCycles:number;totalCases:number;minimumPassed:number;cycles:BenchmarkCampaignCycle[]};
export type BenchmarkSnapshot={cases:BenchmarkCase[];results:BenchmarkResult[];successRate:number;campaign:BenchmarkCampaign|null;releaseReady:boolean};
const MAX_STORED_RESULTS=200,MAX_FAILURE_CHARS=8_000;

export const benchmarkCases:BenchmarkCase[]=[
 {id:'notes',category:'Productivity',name:'Searchable notes',prompt:'Build a desktop notes app where I can create a note, edit its title and body, and search notes. Use local storage and a polished dark interface. Include a button labeled New note, a title field labeled Note title, and a search field labeled Search notes.',workflow:[{action:'click',target:'New note',value:''},{action:'fill',target:'Note title',value:'Baseline note'},{action:'fill',target:'Search notes',value:'Baseline note'},{action:'assert_text',target:'',value:'Baseline note'}]},
 {id:'tasks',category:'Productivity',name:'Task manager',prompt:'Build a desktop task manager with add, complete, filter, and priority controls. Persist tasks locally. Include a text field labeled Task name and a button labeled Add task.',workflow:[{action:'fill',target:'Task name',value:'Plan design review'},{action:'click',target:'Add task',value:''},{action:'assert_text',target:'',value:'Plan design review'}]},
 {id:'expenses',category:'Finance',name:'Expense tracker',prompt:'Build an offline expense tracker with an add-expense form, category filtering, totals, and local persistence. Include fields labeled Description and Amount and a button labeled Add expense.',workflow:[{action:'fill',target:'Description',value:'Coffee beans'},{action:'fill',target:'Amount',value:'18.50'},{action:'click',target:'Add expense',value:''},{action:'assert_text',target:'',value:'Coffee beans'}]},
 {id:'inventory',category:'Business',name:'Inventory manager',prompt:'Build a desktop inventory manager that can add products, adjust quantities, search records, and highlight low stock. Include a button labeled Add product, form fields labeled Product name, SKU, and Category, and a submit button labeled Save product.',workflow:[{action:'click',target:'Add product',value:''},{action:'fill',target:'Product name',value:'Aurora Desk Lamp'},{action:'fill',target:'SKU',value:'LAMP-42'},{action:'fill',target:'Category',value:'Lighting'},{action:'click',target:'Save product',value:''},{action:'assert_text',target:'',value:'Aurora Desk Lamp'}]},
 {id:'timer',category:'Utilities',name:'Focus timer',prompt:'Build a focus timer with start, pause, reset, configurable durations, session history, and desktop-friendly styling. Include buttons labeled Start and Pause and show visible status text Running and Paused.',workflow:[{action:'click',target:'Start',value:''},{action:'assert_text',target:'',value:'Running'},{action:'click',target:'Pause',value:''},{action:'assert_text',target:'',value:'Paused'}]},
 {id:'bookmarks',category:'Utilities',name:'Bookmark organizer',prompt:'Build an offline bookmark organizer with add, edit, tag, favorite, and search workflows. Include a button labeled Add bookmark, fields labeled Bookmark title and Bookmark URL, and a button labeled Save bookmark.',workflow:[{action:'click',target:'Add bookmark',value:''},{action:'fill',target:'Bookmark title',value:'Foundry docs'},{action:'fill',target:'Bookmark URL',value:'https://example.com'},{action:'click',target:'Save bookmark',value:''},{action:'assert_text',target:'',value:'Foundry docs'}]},
 {id:'crm',category:'Business',name:'Simple CRM',prompt:'Build a local CRM with contacts, status stages, search, notes, and an add-contact workflow. Include a button labeled Add contact, fields labeled Contact name and Email, and a button labeled Save contact.',workflow:[{action:'click',target:'Add contact',value:''},{action:'fill',target:'Contact name',value:'Morgan Lee'},{action:'fill',target:'Email',value:'morgan@example.com'},{action:'click',target:'Save contact',value:''},{action:'assert_text',target:'',value:'Morgan Lee'}]},
 {id:'habits',category:'Personal',name:'Habit tracker',prompt:'Build a habit tracker with add habit, daily completion, streaks, and a seven-day summary stored locally. Include a field labeled Habit name and a button labeled Add habit.',workflow:[{action:'fill',target:'Habit name',value:'Evening walk'},{action:'click',target:'Add habit',value:''},{action:'assert_text',target:'',value:'Evening walk'}]},
 {id:'journal',category:'Personal',name:'Daily journal',prompt:'Build a private offline journal with new entries, date navigation, search, autosave, and a calm dark theme. Include a button labeled New entry and fields labeled Entry title and Entry body.',workflow:[{action:'click',target:'New entry',value:''},{action:'fill',target:'Entry title',value:'A productive day'},{action:'fill',target:'Entry body',value:'Completed the Foundry benchmark.'},{action:'assert_text',target:'',value:'A productive day'}]},
 {id:'dashboard',category:'Analytics',name:'Sales dashboard',prompt:'Build a sales dashboard with editable sample data, summary cards, filtering, a recent-orders table, and local persistence. Include fields labeled Customer name and Order amount and a button labeled Add order.',workflow:[{action:'fill',target:'Customer name',value:'Northwind Studio'},{action:'fill',target:'Order amount',value:'425'},{action:'click',target:'Add order',value:''},{action:'assert_text',target:'',value:'Northwind Studio'}]}
];

export class BenchmarkService{
 private readonly campaignFile:string;
 constructor(private readonly resultsFile:string,campaignFile=join(dirname(resultsFile),'campaign.json')){this.campaignFile=campaignFile}
 async snapshot():Promise<BenchmarkSnapshot>{const results=await this.read(),campaign=await this.readCampaign(),latest=new Map<string,BenchmarkResult>();for(const result of results)if(!latest.has(result.benchmarkId))latest.set(result.benchmarkId,result);const completed=[...latest.values()],passed=completed.filter(result=>result.passed).length;return{cases:benchmarkCases,results,successRate:completed.length?Math.round(passed/completed.length*100):0,campaign,releaseReady:campaign?.status==='passed'}}
 async record(result:BenchmarkResult):Promise<BenchmarkSnapshot>{const normalized=this.normalize(result.passed?result:{...result,failureStage:result.failureStage??classifyBenchmarkFailure(result.failure??'')});const results=await this.read();await mkdir(dirname(this.resultsFile),{recursive:true});await writeFile(this.resultsFile,JSON.stringify([normalized,...results].slice(0,MAX_STORED_RESULTS),null,2),'utf8');return this.snapshot()}
 async startCampaign(id:string,totalCycles=3,minimumPassed=9):Promise<BenchmarkCampaign>{const campaign:BenchmarkCampaign={id,status:'running',startedAt:new Date().toISOString(),currentCycle:1,currentCase:0,totalCycles,totalCases:benchmarkCases.length,minimumPassed,cycles:[]};await this.writeCampaign(campaign);return campaign}
 async updateCampaign(campaign:BenchmarkCampaign):Promise<BenchmarkCampaign>{await this.writeCampaign(campaign);return campaign}
 async finishCampaignCycle(campaign:BenchmarkCampaign,cycle:number,startedAt:string,results:BenchmarkResult[]):Promise<BenchmarkCampaign>{const passed=results.filter(result=>result.passed).length,total=results.length,summary:BenchmarkCampaignCycle={cycle,startedAt,completedAt:new Date().toISOString(),passed,total,successRate:total?Math.round(passed/total*100):0,qualified:total===benchmarkCases.length&&passed>=campaign.minimumPassed},cycles=[...campaign.cycles.filter(item=>item.cycle!==cycle),summary].sort((a,b)=>a.cycle-b.cycle),complete=cycle>=campaign.totalCycles,status=complete?(cycles.every(item=>item.qualified)?'passed':'failed'):'running',next={...campaign,cycles,currentCycle:complete?cycle:cycle+1,currentCase:complete?benchmarkCases.length:0,status,...(complete?{completedAt:new Date().toISOString()}:{})} as BenchmarkCampaign;await this.writeCampaign(next);return next}
 async cancelCampaign(campaign:BenchmarkCampaign):Promise<BenchmarkCampaign>{const next:BenchmarkCampaign={...campaign,status:'cancelled',completedAt:new Date().toISOString()};await this.writeCampaign(next);return next}
 case(id:string):BenchmarkCase{const value=benchmarkCases.find(item=>item.id===id);if(!value)throw new Error('Unknown benchmark case.');return value}
 private async read():Promise<BenchmarkResult[]>{try{const value=JSON.parse(await readFile(this.resultsFile,'utf8'));if(!Array.isArray(value))return[];const source=(value as BenchmarkResult[]).slice(0,MAX_STORED_RESULTS),results=source.map(result=>this.normalize(result)),needsCompaction=value.length!==results.length||source.some((result,index)=>result.failure!==results[index].failure);if(needsCompaction){await mkdir(dirname(this.resultsFile),{recursive:true});await writeFile(this.resultsFile,JSON.stringify(results,null,2),'utf8')}return results}catch(error){if((error as NodeJS.ErrnoException).code==='ENOENT')return[];throw error}}
 private async readCampaign():Promise<BenchmarkCampaign|null>{try{return JSON.parse(await readFile(this.campaignFile,'utf8')) as BenchmarkCampaign}catch(error){if((error as NodeJS.ErrnoException).code==='ENOENT')return null;throw error}}
 private async writeCampaign(campaign:BenchmarkCampaign):Promise<void>{await mkdir(dirname(this.campaignFile),{recursive:true});await writeFile(this.campaignFile,JSON.stringify(campaign,null,2),'utf8')}
 private normalize(result:BenchmarkResult):BenchmarkResult{return typeof result.failure==='string'&&result.failure.length>MAX_FAILURE_CHARS?{...result,failure:`${result.failure.slice(0,MAX_FAILURE_CHARS)}\n[Diagnostic truncated by Foundry]`}:result}
}

export function classifyBenchmarkFailure(message:string):BenchmarkFailureStage{const value=message.toLowerCase();if(/compile|typescript|syntax|build failed/.test(value))return'compile';if(/persist|reload|localstorage/.test(value))return'persistence';if(/workflow|visible text|text field|button labeled|could not find/.test(value))return'workflow';if(/screenshot|visual|layout|contrast|viewport/.test(value))return'visual';if(/runtime|rendered no visible|startup|epipe|process/.test(value))return'runtime';if(/openai|agent|safety limit|api|fetch|connection/.test(value))return'generation';return'unknown'}
